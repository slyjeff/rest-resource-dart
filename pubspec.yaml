name: rest_resource
description: Client-side HATEOAS support for Flutter apps consuming SlySoft REST resources.
version: 0.1.0
environment:
  sdk: '>=3.0.0 <4.0.0'
dependencies:
  http: ^1.2.0
dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
